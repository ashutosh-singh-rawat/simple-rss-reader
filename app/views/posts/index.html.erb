<div class="container">
  <% @posts.each do |post| %>
    <div class="panel panel-default">
      <div class="panel-body">
        <%= link_to post.title, [@feed, post] %> - <i> published <%= time_ago_in_words(post.published_at) %> ago.</i>
      </div>
    </div>
  <% end %>
</div>